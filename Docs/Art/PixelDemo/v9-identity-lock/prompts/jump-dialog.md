Use case: identity-preserve
Asset type: final jump and dialogue-interaction sprite sheet for the Douhua macOS desktop pet
Input images: Image 1 is the final runtime idle sprite and is the ABSOLUTE identity/anatomy master. Image 2 is the canonical model sheet, secondary. Image 3 is the real Douhua photo board for individual markings only. Image 4 is the rejected prior action contact sheet, supplied only to show the failure: the crouch/jump changes the cat's proportions. Image 5 is the old jump/dialog sheet, pose-order reference only.

Primary request: create exactly eight progressive right-facing full-body frames of THIS EXACT IDLE CAT, arranged four columns by two rows. Copy the idle cat's exact head pixel dimensions, cheek width, ear spacing, eye placement, muzzle length, neck/chest thickness, ribcage depth, hip volume, limb thickness, paw size, tail thickness, coat color, markings, rendering treatment, and pixel density into every frame. Change articulation only.

Top row, one small natural jump: 1 the exact idle body bends its existing joints into a planted crouch without shrinking, 2 hind legs extend in upward push while the same chest/head rise, 3 compact airborne tuck with all four complete paws visible and the same head/ribcage mass, 4 four-paw landing compression that can return directly to the exact idle master. It must look like one cat squashing and stretching through joints and spine, never like four differently scaled cats.

Bottom row, dialogue interaction: 5 exact neutral idle identity, 6 same cat looks upward, 7 same cat raises the near forepaw, 8 same cat reaches/holds that forepaw toward an external dialogue bubble. Do not draw the bubble. The unraised limbs, skull, chest and rump keep the idle master volume.

Hard invariants: fixed skull size, face width, chest depth, hip volume and paw size in all eight frames; one identical character and one identical pixel scale. No pose may use whole-character resizing. Full anatomy and four limbs must read correctly.

Composition: eight separate complete cats; bottom row shares a fixed ground baseline; generous padding around ears, face, raised paw, grounded paws and tail; no overlap or crop. Perfectly flat solid #ff00ff background with no floor, shadow, motion effects, bubble, grid, text or watermark.

Avoid: redesigning or reinterpreting the cat, generic orange tabby, smaller jump head, oversized dialogue head, narrow chest, tall human-like torso, long thin legs, changing scale, missing/merged legs, detached paw, cut-off face/ear/paw/tail.
