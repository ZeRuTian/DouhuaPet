# v0.10 独立视觉 QA

结论：`pass`

角色身份、比例与像素尺度跨落座、驻留、拍边缘保持一致；四肢完整，落座与拍爪关节连续；驻留、贴边、窗口跟随和跳返自然，未见裁切、残片或闪烁。

修复记录：首轮 `window-tap` 因起始坐姿明显宽于批准的驻留姿态而被拒绝。最终版重新生成完整八帧，未使用逐帧缩放修补；三处状态边界的 alpha 轮廓差为 `1.31%–1.47%`，宽高偏差不超过 `2 px`。
